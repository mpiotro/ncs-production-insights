"""ncs — NCS Production Insights.

Package marker for the SODIR ingestion layer (phase 001). Models and the
fetch -> normalize -> link -> persist -> report pipeline land in later tasks
(001-T7 onward); this module carries only the package version.
"""

__version__ = "0.1.0"
