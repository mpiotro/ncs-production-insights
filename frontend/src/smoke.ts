// Trivial pure module exercised by the 004-T1 smoke test, so the V8 coverage table has
// real source to report and proves the runner + coverage instrumentation are wired (R6).
// Replaced by real source (lib/*, api/*, components/*) in later 004 tasks.
export function scaffoldOk(): boolean {
  return true;
}
