// Registers @testing-library/jest-dom matchers (toBeInTheDocument, toHaveTextContent, …)
// on Vitest's expect, for every test in the suite (004-R6 test apparatus).
import "@testing-library/jest-dom/vitest";
