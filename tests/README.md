# tests/

- `acceptance/` — acceptance tests authored by the **test-author** from each phase's EARS criteria,
  before the implementation exists.
- `unit/` — unit tests authored by the **developer** alongside the code.

See `specs/roadmap.md`. The split is enforced by the role-guard hook (`.claude/hooks/`).
